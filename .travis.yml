# testing ppc64le.
language: c

#matrix:
#  include:
#    - os: linux-ppc64le
#      dist: xenial
#      sudo: false
#    - os: linux
#      dist: trusty
#      sudo: required

os:
  - linux-ppc64le
  - linux

sudo: required
dist: trusty

before_install:
 - sudo apt-get -qq update
 - sudo apt-get install -y autopoint libuv-dev libdevmapper-dev flex bison
 - sudo ./bootstrap

install:
 - sudo ./configure --with-twin-x11=no --with-twin-fbdev=no
 - sudo make

script:
 - sudo make check
